{
    "name": "aidantwoods/betteroptions",
    "description": "Better than `getopt()`",
    "keywords": [
        "betteroptions",
        "better",
        "options",
        "getopt",
        "php"
    ],
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Aidantwoods\\BetterOptions\\": ["src/"]
        }
    },
    "require": {
        "php": ">=7.0"
    },
    "suggest": {
        "symfony/yaml": "To support config via YAML files"
    },
    "require-dev": {
        "symfony/yaml": "^3.2"
    }
}
